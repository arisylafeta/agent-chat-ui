import localFont from 'next/font/local';

// Centralized local fonts registry
// Use exported font.className in className strings

export const melia = localFont({
      src: './Melia.otf',
  display: 'swap',
});
// comments